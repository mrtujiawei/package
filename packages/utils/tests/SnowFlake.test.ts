// import { SnowFlake } from '../src';

describe('SnowFlake test', () => {
  // const snowFlake = new SnowFlake((1 << 5) - 1, (1 << 5) - 1);
  test('generateNextId test', () => {
    // console.log(snowFlake.generateNextId());
  });
});
