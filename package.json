{
  "private": true,
  "workspaces": [
    "packages/utils",
    "packages/node-utils",
    "packages/bin",
    "packages/web-utils"
  ],
  "scripts": {
    "dev": "yarn workspaces run dev",
    "build": "yarn workspaces run build"
  }
}
