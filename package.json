{
  "private": true,
  "workspaces": [
    "packages/utils",
    "packages/node-utils",
    "packages/bin",
    "packages/web-utils"
  ],
  "scripts": {
    "start": "yarn workspaces run start",
    "build": "yarn workspaces run build",
    "pub": "yarn workspaces run pub"
  }
}
